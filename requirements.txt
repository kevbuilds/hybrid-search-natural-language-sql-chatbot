# Core dependencies for Hybrid SQL RAG Chatbot
# Tested and working versions for Streamlit Cloud deployment

# AI & LLM
anthropic==0.72.0          # Claude API client (upgraded for httpx compatibility)

# Database
psycopg[binary]==3.2.3     # PostgreSQL adapter for Neon (Python 3.13 compatible)

# Web Framework
streamlit==1.39.0          # Web UI framework
pandas==2.2.0              # Data manipulation

# Embeddings & Vector Search
chromadb==0.5.23           # Vector database for RAG (Python 3.13 compatible)
sentence-transformers==3.3.1  # Embedding model (all-MiniLM-L6-v2)

# Utilities
python-dotenv==1.0.0       # Environment variables (local dev only)

